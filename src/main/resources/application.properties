# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/user-api-db

# Server port (optional, default is 8080)
server.port=8080

# Enable Spring Boot logs (optional but helpful for debugging)
logging.level.org.springframework=INFO
